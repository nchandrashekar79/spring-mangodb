spring:
  application:
    name: spring-mongodb
  mongodb:
    uri: ${MONGODB_URI:mongodb://localhost:27017/products}
server:
  port: ${SERVER_PORT:8080}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs